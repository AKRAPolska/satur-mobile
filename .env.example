# Copy to .env (optional). Makefile defaults work without it.
# Backend = ../satur Sylius — NO database in this repo.

# Host / Chrome / physical device on same LAN (adjust IP if needed)
API_BASE_URL=http://localhost:5051

# Android emulator loopback to host
API_BASE_URL_ANDROID_EMU=http://10.0.2.2:5051

# Toolchain (usually set in ~/.bashrc already)
# FLUTTER_ROOT=/home/daniel/development/flutter
# ANDROID_HOME=/home/daniel/Android/Sdk
